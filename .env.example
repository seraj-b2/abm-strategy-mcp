# Server Configuration (Backend API URL for MCP Token verification)
BACKEND_URL=http://localhost:5000

# Transport type: stdio or sse
TRANSPORT=stdio

# Port for SSE transport
PORT=3000

# Skip authentication check (true/false)
SKIP_AUTH=false
